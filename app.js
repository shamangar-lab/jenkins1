app.js

'use strict';

const express = require('express');
const app = express();

// Server connection
const PORT = 9000;
const HOST = '0.0.0.0';

// Application content
const os = ['windows', 'macOS', 'Linix']

// Web Server
app.get('/'.function(reg.res){
});

// Console output
app.listen(PORT, HOST);
console.log('Running on http://${HOST}:${PORT}');